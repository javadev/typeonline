/*jshint browser: true*/
/*global require,define*/
require.config({
  paths: {
    jquery: 'external/jquery',
    underscore: 'external/underscore'
  }
});

define([
],
function () {
  'use strict';

});
